#include "Component/TextButton.hpp"

TextButton::TextButton(
    int size,
    std::string& text,
    float zIndex):
    Text(
        size,
        text,
        zIndex),
    GameObject(

        ){

}
